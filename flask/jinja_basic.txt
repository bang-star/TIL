#jinja_basic

변수 : 변수는 {% set 변수명 = 변수 값 %}으로 설정
조건문 : if문이 어디서 끝나는지 알 수 없으므로 {% endif %} 사용해주어야 한다.
for문 : for문이 어디서 끝나는지 알 수 없으므로 {% endfor %} 사용해주어야 한다.

이외에도 title, script, 다른 html 문서를 만들어 끼어 넣는 것 등 다양한 방법으로 사용가능하다.